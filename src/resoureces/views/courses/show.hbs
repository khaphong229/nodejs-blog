<div class="mt-4">
    <div class="row">
        <div class="col-lg-3">
            <button>Learn now</button>
        </div>
        <div class="col-lg-3">
            <h2>{{course.name}}</h2>
            <img src="{{course.image}}" alt="" style="width: 100%">
            <p>{{course.description}}</p>
            <a href="/courses/{{course._id}}/update" class="btn btn-primary">Update</a>
            <a href="/courses/{{course._id}}" class="btn btn-primary">Delete</a>
        </div>
    </div>
</div>